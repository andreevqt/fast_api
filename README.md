# [FastAPI](https://fastapi.tiangolo.com) example project

```sh
# to run todo app
cd todo_app
docker-compose down && docker-compose up --build

# to run shorten url app
cd shorten_url_app
docker-compose down && docker-compose up --build
```
